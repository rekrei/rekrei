box: ruby:2.3.1
services:
  - id: postgresql
    env:
      - POSTGRES_PASSWORD: ourlittlesecret
build:
  steps:
    - rails-database-yml
    - bundle-install
    - script:
        name: rspec
        code: bundle exec rspec
