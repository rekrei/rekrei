box: ruby:2.3.1
service:
  - id: postgres
    env:
      - POSTGRES_PASSWORD: ourlittlesecret
build:
  steps:
    - rails-database-yml
    - bundle-install
    - script:
        name: rspec
        code: bundle exec rspec
