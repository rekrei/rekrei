box: ruby:2.3.1
services:
   - id: postgres
     env:
       POSTGRES_PASSWORD: supersecret
       POSTGRES_USER: root
       POSTGRES_DB: rekrei_test
build:
  steps:
    - rails-database-yml
    - bundle-install
    - script:
        name: rspec
        code: bundle exec rspec
