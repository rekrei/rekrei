= form_for [@artefact, @sketchfab] do |f|
  .control_group
    = f.label :bbcode, 'BBCode'
    .controls
      = f.text_area :bbcode, cols: 40, rows: 5
  .control_group
    .controls
      = f.submit
